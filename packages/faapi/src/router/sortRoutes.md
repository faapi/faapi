# sortRoutes

一句话概括：对路由清单排序，确保匹配优先级正确。

## 为什么需要

静态路由应该优先于动态路由匹配，否则 `/user/login` 会被 `/user/:id` 错误匹配。排序确保静态路由在前，动态路由在后。

## 使用场景

- 路由扫描后排序
- 确保匹配顺序正确

## 相关模块

- `scanRoutes.ts` - 扫描后调用排序
- `matchRoute.ts` - 依赖排序结果
