# matchRoute

一句话概括：根据请求匹配路由，支持静态和动态路由。

## 为什么需要

HTTP 请求到达后，需要找到对应的路由处理函数。支持静态路由（如 `/auth/login`）和动态路由（如 `/user/:id`），并提取动态参数。

## 使用场景

- HTTP 请求到达时匹配路由
- 提取动态路由参数
- 返回匹配结果供后续处理

## 相关模块

- `routeTypes.ts` - 输入输出类型
- `sortRoutes.ts` - 依赖排序后的路由清单
