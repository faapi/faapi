# parseArgs

一句话概括：解析 CLI 命令行参数。

## 为什么需要

用户通过命令行启动服务，需要解析参数（端口、路由模式、app 目录等），转换为结构化配置。

## 使用场景

- CLI 启动时解析参数
- 支持选项和位置参数
- 返回结构化配置对象

## 相关模块

- `normalizePatterns.ts` - 标准化路由模式
- `startCommand.ts` - 使用解析结果
